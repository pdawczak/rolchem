{% extends 'AppAppBundle::admin_layout.html.twig' %}

{% block body %}
    <div class="page-header">
        <h1>Edycja produktu <small>{{ product.name }}</small></h1>
    </div>

    {% include 'AppAdminProductBundle:Default:_form.html.twig' with { form: form, action: path('admin_product_edit', { productId: product.id }) } %}
{% endblock body %}
