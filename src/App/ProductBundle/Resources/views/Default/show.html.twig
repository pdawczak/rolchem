{% extends 'AppAppBundle::app_layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-5">
            <img class="thumbnail" src="{{ vich_uploader_asset(product, 'image') | imagine_filter('product_details') }}" />
        </div>
        <div class="col-md-7">
            <div class="page-header">
                <h1>{{ product.name }}</h1>
            </div>
            {{ product.description|raw }}

            <a href="{{ path('app_order_addItem', { productId: product.id }) }}" class="add-item btn btn-primary">
                {{ icon('plus') }} Dodaj do zamówienia
            </a>
        </div>
    </div>
{% endblock body %}
